services:
  app:
    build: .
    container_name: hello-aws
    env_file:
      - .env          # SSH로 넣어둔 AWS 자격증명/큐 설정을 컨테이너에 주입
    ports:
      - "127.0.0.1:8080:8080"   # nginx가 80/443을 받아 localhost:8080으로 프록시
    restart: unless-stopped
